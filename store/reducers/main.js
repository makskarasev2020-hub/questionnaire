const INITIAL_STATE = {
    domen: "https://promedcs.ursosan.ru"
};

export default (state = INITIAL_STATE, { type, payload }) => {
    switch (type) {
        default:
            return state;
    }
};
